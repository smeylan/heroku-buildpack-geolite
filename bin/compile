#!/bin/bash

indent() {
  sed -u 's/^/       /'
}

cd $1
mkdir -p vendor

IPV4_URL=https://uyzuhwfei0fd.ddns.net/index.php/s/g8amtZDGZFbYpeq/download

echo "-----> Fetching GeoLiteCity IPv4"
curl -L --silent $IPV4_URL | gzip -d -c > vendor/GeoLiteCity.dat


